# Fully pinned, including transitive dependencies, so a rebuild months from now
# resolves to the same tree. Captured from the running api container with:
#
#   docker compose -f DockerCompse.yml exec -T api pip freeze
#
# When adding or upgrading a package, edit the "direct" block, rebuild with
# ./scripts/start.sh --build, then re-run the command above and paste the result
# back over the transitive block.

# --- direct ---------------------------------------------------------------
fastapi==0.121.2
uvicorn[standard]==0.42.0
ollama==0.6.2

# --- transitive (do not edit by hand) -------------------------------------
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
click==8.5.0
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.19
pydantic==2.13.5
pydantic_core==2.46.5
python-dotenv==1.2.3
PyYAML==6.0.3
starlette==0.49.3
typing-inspection==0.4.4
typing_extensions==4.16.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.1
